# Resources

App resources (asset catalogs, localized strings, etc.) live here. Skip copies
these into both the iOS bundle and the Android assets. This placeholder keeps
the `Resources` directory present for the `.process("Resources")` rule in
`Package.swift`.
